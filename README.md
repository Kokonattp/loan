# 💰 ระบบบันทึกเงินกู้ (Loan Record System)

Next.js + Tailwind CSS + Supabase

## ✨ ฟีเจอร์

- 📊 แสดงยอดรวมสัปดาห์ + Progress Bar
- 💰 บันทึกเงินกู้ (ชื่อ + ยอดเงิน)
- ⚠️ จำกัดวงเงิน 60,000 บาท/สัปดาห์
- 📋 ดูประวัติทั้งหมด
- 🎨 UI สวย Glass Morphism + Tailwind CSS

## 🚀 วิธี Deploy

### 1. เพิ่มตารางใน Supabase (โปรเจคเดิมของลาพักร้อน)

ไปที่ **SQL Editor** แล้วรัน `supabase-setup.sql` เพื่อสร้างตาราง `loan_records`

> ⚠️ ใช้ Supabase โปรเจคเดียวกับระบบลาพักร้อน (credentials ใส่ไว้ให้แล้ว)

### 2. รัน Next.js

```bash
npm install
npm run dev
```

### 3. Deploy to Vercel

```bash
vercel
```

## 📁 โครงสร้างไฟล์

```
loan-record-nextjs/
├── app/
│   ├── globals.css      # Tailwind + Custom styles
│   ├── layout.js        # Root layout
│   └── page.js          # Main page component
├── package.json
├── tailwind.config.js
├── postcss.config.js
├── next.config.js
├── supabase-setup.sql   # SQL สำหรับสร้างตาราง
└── README.md
```

## ⚙️ ปรับแต่ง

### เปลี่ยนวงเงินต่อสัปดาห์

แก้ไข `app/page.js` บรรทัด 10:
```javascript
const WEEKLY_LIMIT = 60000;
```

### เปลี่ยนสี Theme

แก้ไข `tailwind.config.js` ในส่วน `colors`

### เปลี่ยน Background Gradient

แก้ไข `app/globals.css` ในส่วน `body`
